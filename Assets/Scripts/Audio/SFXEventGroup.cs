using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "NewSFXEventGroup", menuName = "Scriptable Objects/Audio/SFXEventGroup", order = 1)]
public class SFXEventGroup : AudioEventGroup
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
